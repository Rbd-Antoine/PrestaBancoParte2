spring:
  application:
    name: simulacionCredito-service
  config:
    enabled: true
    uri: http://localhost:8888
  datasource:
    url: jdbc:mysql://localhost:3306/simulacion-db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
