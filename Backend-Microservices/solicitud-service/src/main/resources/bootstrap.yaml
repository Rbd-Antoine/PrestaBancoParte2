spring:
  application:
    name: solicitud-service
  config:
    enabled: true
    uri: http://localhost:8888
  datasource:
    url: jdbc:mysql://localhost:3306/creditos-db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect